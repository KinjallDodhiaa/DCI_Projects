/**1- There is an authentication function that takes [IBAN & pincode] and returns a JSON string "IBAN and the token number" (the token must be a random 
 * number).
    For instance:   "IBAN=DE000000000  picode=1234"
 */

 let iban = document.querySelector('#iban');
 